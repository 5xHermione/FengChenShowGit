<p id="notice"><%= notice %></p>

<h1>所有專案</h1>

<table>
  <thead>
    <tr>
      <th>標題</th>
      <th>描述</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @repositories.each do |repository| %>
      <tr>
        <td><%= repository.title %></td>
        <td><%= repository.description %></td>
        <td><%= repository.user.email %></td>
        <td><%= link_to 'Show', repository %></td>
        <td><%= link_to 'Edit', edit_repository_path(repository) %></td>
        <td><%= link_to 'Destroy', repository, method: :delete, data: { confirm: '確定刪除？' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>

<%= link_to 'New Repository', new_repository_path %>
